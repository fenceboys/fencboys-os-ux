/**
 * Centralized sizing constants for consistent UI dimensions
 */

// Table column minimum widths
export const TABLE_MIN_WIDTHS = {
  dropdown: '120px',
  salesperson: '140px',
} as const;

// Progress tracker dimensions
export const PROGRESS_TRACKER = {
  normalWidth: '100px',
  compactWidth: '56px',
} as const;

// Calendar/Dashboard dimensions
export const DASHBOARD = {
  calendarCellMinHeight: '100px',
} as const;
